// import { CommonViewProps } from '@base/containers/ViewField/Common/interface';
// import { User } from '@directory/users/types/user';
// import { Box } from '@mui/material';
// import UserContact from '../../UserContact';

// interface ViewProps extends CommonViewProps {
//   value: User;
//   menuSourceId: string;
//   menuSource: string;
// }

// const View = (props: ViewProps) => {
//   const { value, menuSourceId, menuSource } = props;
//   return (
//     <Box sx={{ width: '100%' }}>
//       <UserContact mode={'view'} layoutData={value} menuSourceId={menuSourceId} menuSource={menuSource} />
//     </Box>
//   );
// };

// export default View;
