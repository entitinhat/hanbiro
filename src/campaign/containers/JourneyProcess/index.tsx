import { Box } from '@mui/material';

interface JourneyProcessProps {
  menuSource?: string;
  menuCategory: string;
  menuSourceId: string;
}

const JourneyProcess = (props: JourneyProcessProps) => {
  return <Box>AAA</Box>;
};

export default JourneyProcess;
