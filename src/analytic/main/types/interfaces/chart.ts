import {AssignUserProps, DateRangeProps} from "@analytic/main/components/Filter";
import {GroupProps} from "react-select";

/*
export interface ChartFilter {
  isShow?: boolean;
  dateProps?: DateRangeProps;
  userProps?: AssignUserProps;
  groupProps?: GroupProps;
  isDateUser?: boolean;
  isDateGroup?: boolean;
}

export interface Chart {
  key: string;
  title: string;
  toolbarProps?: any;
  filterProps?: ChartFilter;
  adminProps?: Chart;
  [x: string]: any;
}*/
