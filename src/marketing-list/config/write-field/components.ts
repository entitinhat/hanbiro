import { lazy } from 'react';

export const UserAutoComplete = lazy(() => import('@sign-in/containers/UserAutoComplete'));
export const ActiveAutoComplete = lazy(() => import('@marketing-list/containers/ActiveAutoComplete'));
